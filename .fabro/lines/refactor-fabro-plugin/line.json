{
  "version": 1,
  "name": "Refactor Fabro plugin",
  "workflow": "workflow.fabro",
  "files": [
    "workflow.fabro",
    "runner.mjs"
  ],
  "environment": "local",
  "autoApprove": true,
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "outputSchema": {
    "type": "object",
    "required": [
      "baseSha",
      "resultSha",
      "changedFiles",
      "before",
      "after",
      "review",
      "outcome"
    ],
    "properties": {
      "baseSha": {
        "type": "string",
        "pattern": "^[a-f0-9]{40}$"
      },
      "resultSha": {
        "type": "string",
        "pattern": "^[a-f0-9]{40}$"
      },
      "changedFiles": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "before": {
        "type": "object"
      },
      "after": {
        "type": "object"
      },
      "review": {
        "type": "string",
        "minLength": 1
      },
      "outcome": {
        "enum": [
          "changed",
          "no_change"
        ]
      }
    }
  },
  "acceptanceInstructions": "Independently inspect the plugin diff and verify each criterion, unchanged behavior, public SDK compatibility, existing job/card compatibility, and clearer code. Typecheck, unit tests, build, and whitespace checks must pass. Changes must stay inside plugins/bb-plugin-assembly-lines; do not weaken tests or alter package scripts or test/compiler configuration. Explain measurable or concrete code-quality improvement. No merge or publication."
}
