import { definePluginApp } from "@get-bb/plugin-sdk/app";

/**
 * Attractor app entry point.
 *
 * Scaffold (task T1): registers no slots yet. T5 adds the
 * `attractor-run` message directive and thread-panel action (the live DAG
 * view), and T6 adds the human-gate pendingInteraction renderer — see
 * docs/plans/2026-09-13-attractor-runner-plan.md.
 */
export default definePluginApp(() => {});
