{
  "version": 1,
  "baselineCommit": "ee79ab4",
  "formula": "moduleDebt=sum(max(0,tokenLines(file)-500)); functionDebt=sum(max(0,tokenLines(function)-150)); totalDebt=moduleDebt+functionDebt",
  "fileScope": ["app.tsx", "server.ts", "transport.ts", "canvas/**/*.ts(x)", "hooks/**/*.ts(x)", "lib/**/*.ts(x)"],
  "note": "Re-baselined 2026-09-15 on bb/bb-thread-frame-control: the launch-or-attach cleanup (commit dc70212) plus this branch's bbthread shape work moved productionTokenLines/decisionPoints far enough (deletion, then a smaller addition) that the size-drift invariants below needed a fresh reference point. moduleDebt/functionDebt/totalDebt are DELIBERATELY left pointing at the original ee79ab4 high-debt snapshot (11303 lines, 5273 total debt) rather than reset to this branch's own near-zero debt — the improvement percentage is a standing 'how far we've come since ee79ab4' record, and resetting it to a current already-low number would make every future compare read 0% improvement (an unchanged, undegraded debt level) and fail a check that exists to catch NEW debt, not to demand improvement on top of improvement. Only the three drift invariants (productionTokenLines, decisionPoints, localImportCycleCount) are refreshed here.",
  "invariants": { "productionTokenLines": 9835, "decisionPoints": 1149, "localImportCycleCount": 0 },
  "productionTokenLines": 9835,
  "decisionPoints": 1149,
  "localImportCycleCount": 0,
  "moduleDebt": 2990,
  "functionDebt": 2283,
  "totalDebt": 5273
}
