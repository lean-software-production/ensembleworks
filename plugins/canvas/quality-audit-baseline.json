{
  "version": 1,
  "baselineCommit": "3b80925",
  "note": "RE-FROZEN AT W18 (2026-09-10), the second and final re-freeze of this feature \u2014 see D10 in artifacts/harness/c89c66dd/plan.md. productionTokenLines 16610 -> 17695 and decisionPoints 1770 -> 1850, the discovery tree's real size once the node inspector and on-canvas state marks landed. W14 froze the first eleven nodes; this freezes the four that followed the C3 critique and the owner's own testing. Both are deliberate operator acts, not per-node re-baselining. totalDebt is STILL NOT re-frozen and still carries ee79ab4's 5273: compare() computes improvement as (frozen.totalDebt - current)/frozen.totalDebt, so freezing it pins improvement to 0% forever. With a fixed divisor the 90% floor is an ABSOLUTE ceiling of ~527 debt units, which does not go stale and must never be re-baselined. It currently reads 93.48% (debt 344) and passes on its own merits.",
  "formula": "moduleDebt=sum(max(0,tokenLines(file)-500)); functionDebt=sum(max(0,tokenLines(function)-150)); totalDebt=moduleDebt+functionDebt",
  "fileScope": [
    "app.tsx",
    "server.ts",
    "transport.ts",
    "canvas/**/*.ts(x)",
    "hooks/**/*.ts(x)",
    "lib/**/*.ts(x)"
  ],
  "invariants": {
    "productionTokenLines": 17695,
    "decisionPoints": 1850,
    "localImportCycleCount": 0
  },
  "productionTokenLines": 17695,
  "decisionPoints": 1850,
  "localImportCycleCount": 0,
  "moduleDebt": 2990,
  "functionDebt": 2283,
  "totalDebt": 5273
}
