export const meta = {
  name: "communications-marketplace-demo",
  description: "Bounded implementation, independent validation, and feedback loop for the marketplace demo.",
  phases: [{ title: "Implement" }, { title: "Validate" }, { title: "Fix" }],
};
const brief = `Implement the Communications Hub marketplace demo in the CURRENT checkout on branch bb-plugin-decomposition-doc for PR #86. Read AGENTS.md, relevant BB plugin skills, and docs/design/bb-plugin-decomposition-and-distribution.html. The parent will independently validate, commit, publish preview tags, and push the PR; do not commit, push, tag, change branches, install into the active BB instance, or spawn other agents.

Import tracked source files from /home/ensembleworks-agent/bb-plugin-communications-hub at its clean HEAD 725d055 into plugins/communications-hub, keeping its isolated npm package/lockfile and behavior. Exclude .git, node_modules, dist, runtime data, secrets. Read its AGENTS.md, glossary, and MVP spec. Record provenance. Use apply_patch for file creation/edits (a generated patch from git-tracked text files is fine). Do not alter the original checkout.

Add .bb/plugins.json indexing Canvas and Communications Hub; a minimal root marketplace.json for EnsembleWorks with only Communications Hub; a small local icon if required. Implement a scripted release workflow and meaningful tests for guardrails: clean tree, known plugin, version format, no moved tags, main/sync gate for stable releases. This PR needs a separately explicit preview mode that only allows prerelease versions on its feature branch, so the parent can publish two demo prereleases using the script without merging main. Catalog must honestly select these preview versions and document switching to stable later. Use public BB catalog schema and exact CLI from installed guides. Keep release tooling small. Do not hand-tag. Include a no-push/check mode if useful for tests, avoiding untested side effects.

Add focused CI for imported plugin and release/catalog checks. Write concise developer/install instructions including isolated-instance marketplace install, import fixture, thread attachment, read/search/citations, reload persistence, and update verification. Keep this a file-import demonstration: no Zoom setup, LiveKit migration, factory, or presence implementation. Update HTML implementation status only as supported by actual changes. Keep existing root Bun workspaces and Canvas behavior unchanged.

Run npm ci, npm test, npm run typecheck, bb plugin build for the imported plugin; check SDK pins with bb plugin types --check and resolve needed compatibility explicitly. Do not claim live install/update tested unless actually done: parent handles separate-instance smoke. Avoid unrelated changes and source-version upgrades. If Canvas interaction-bearing code is touched, required seeded contracts, RED before fix and BOTH adapters apply; prefer no such edits. Record ux-contract: none rationale for PR.

Return concise changes, commands/results, remaining issues, and exact preview release commands. You share the checkout with parent, who will only inspect while you work.`;
let result = await agent(brief, { provider: "codex", model: "gpt-5.6-luna", reasoningLevel: "high", phase: "Implement", label: "Implement marketplace demo" });
const schema = { type: "object", required: ["passed", "findings", "evidence"], additionalProperties: false, properties: { passed: { type: "boolean" }, findings: { type: "array", items: { type: "string" } }, evidence: { type: "string" } } };
for (let round = 0; round < 3; round++) {
  const review = await agent(`Independently validate the current implementation of this task. Do not edit files, commit, push, publish tags, or mutate the active BB instance. Read the code and run meaningful checks. Inspect release scripts for destructive behavior, cleanliness/sync gates, immutable tags, correct prerelease handling, catalog version selection, and clean managed-install reproducibility. Verify source provenance and no copied secrets/runtime files. Distinguish parent-owned live smoke/release steps from defects. Report concrete blocking findings with reproduction evidence.\nTASK:\n${brief}\nIMPLEMENTER REPORT:\n${result}`, { provider: "codex", model: "gpt-5.6-luna", reasoningLevel: "high", phase: "Validate", label: "Independent validation " + (round + 1), schema });
  if (review.passed && review.findings.length === 0) return { status: "ready-for-parent-validation", review, implementation: result };
  if (round === 2) { log("Stopped after three validation rounds; remaining findings are returned to the parent, not silently ignored."); return { status: "needs-parent-review", review, implementation: result }; }
  result = await agent(`Fix the independently verified issues below, re-run relevant checks, and report evidence. Do not commit/push/tag or mutate active BB. Original task:\n${brief}\nReview:\n${JSON.stringify(review)}`, { provider: "codex", model: "gpt-5.6-luna", reasoningLevel: "high", phase: "Fix", label: "Fix feedback " + (round + 1) });
}
